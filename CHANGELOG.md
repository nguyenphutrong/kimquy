# Changelog

## 0.2.0 - 2026-01-09

### Added
- Add global initialization with `kq init --global` for user-level setup.
- Add global config discovery and merge with project config (project wins).
- Add `kq import` command to import skills from Claude and Cursor.
- Add AI agent setup guide and best practices in docs.
